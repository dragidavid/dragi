import type { NextPage } from "next";

import Layout from "components/Layout";
import Grid from "components/Grid";

const Home: NextPage = () => {
  return (
    <Layout>
      <Grid />
    </Layout>
  );
};

export default Home;
